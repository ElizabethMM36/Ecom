import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.from(
    alpha: 1,
    red: 0.075,
    green: 0.522,
    blue: 0.09,
  );
  static const gradientStart = Color.fromRGBO(20, 212, 65, 0.812);
  static const gradientEnd = Color(0xFF3B82F6);

  static const bg = Color(0xFFF8FAFC);
  static const card = Colors.white;

  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const backgroundStart = Color(0xFFD4DAD8);
  static const backgroundEnd = Color(0xFFEBEFED);

  static const Color glassWhite = Color(0xB3FFFFFF); // 70% Opacity
  static const Color textMain = Color(0xFF2D3436);
  static const Color accentGreen = Color(0xFF3BF673);
}
