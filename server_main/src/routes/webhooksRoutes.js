// Webhooks file - Currently unused (Razorpay integration removed)
const express = require('express');
const router = express.Router();

// All Razorpay webhook endpoints have been removed
// If you implement a new payment gateway in the future, add webhook routes here

module.exports = router;